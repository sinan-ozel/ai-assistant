services:
  # Test infrastructure services
  qdrant-test:
    image: qdrant/qdrant:v1.12.1
    container_name: qdrant-test-no-redis
    networks:
      - test-network
    healthcheck:
      test: ["CMD-SHELL", "timeout 10s bash -c ':> /dev/tcp/127.0.0.1/6333' || exit 1"]
      interval: 5s
      timeout: 3s
      retries: 5

  embedding-test:
    image: sinanozel/ollama.0.12.11:all-minilm-33m
    container_name: embedding-test-no-redis
    networks:
      - test-network
    healthcheck:
      test: ["CMD-SHELL", "timeout 10s bash -c ':> /dev/tcp/127.0.0.1/11434' || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Application under test
  app:
    build:
      context: ../..
      dockerfile: agent_stem/Dockerfile
    container_name: app-test-no-redis
    # network_mode: "host"
    environment:
      - QDRANT_HOST=qdrant-test
      - QDRANT_PORT=6333
      - EMBEDDING_SERVER=http://embedding-test:11434
      - CONVERSATION_WINDOW_LIMIT=2048
      # - LOG_LEVEL=DEBUG
      - OLLAMA_HOST
      - LLAMA_CPP_HOST
      - EMBEDDING_TIMEOUT=1.0
    env_file:
      - .env
    depends_on:
      qdrant-test:
        condition: service_healthy
    networks:
      - test-network
    healthcheck:
      test:
        [
          "CMD",
          "python",
          "-c",
          "import json,urllib.request,sys; \
          r=urllib.request.urlopen('http://localhost:8000/health',timeout=2); \
          d=json.load(r); \
          sys.exit(0)"
        ]
      interval: 5s
      timeout: 3s
      retries: 20
      start_period: 60s
    volumes:
      - ${AGENT_FOLDER:-./cortex}:/app/cortex

  # Test runner
  tests:
    build:
      context: ../..
      dockerfile: test_environments/Dockerfile
    container_name: tests-runner-no-redis
    environment:
      - BASE_URL=http://app:8000
      - OLLAMA_HOST
      - LLAMA_CPP_HOST
    env_file:
      - .env
    depends_on:
      app:
        condition: service_healthy
    networks:
      - test-network
    volumes:
      - ${TEST_FOLDER:-.}:/tests
      - ${AGENT_FOLDER:-./cortex}:/app/cortex
    command: ["pytest", "--openapi=http://app:8000", "--openapi-no-strict-example-checking", "--openapi-markdown-output=report.md", "--openapi-no-stdout", "--openapi-timeout=315", "-vv", "--maxfail=1"]

networks:
  test-network:
    driver: bridge
